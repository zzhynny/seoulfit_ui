/// Template for local API keys.
/// Copy this file to `api_keys.dart` (which is gitignored) and fill in your
/// own keys. `api_keys.dart` must never be committed.
class ApiKeys {
  static const String places = 'YOUR_GOOGLE_PLACES_API_KEY';
}
